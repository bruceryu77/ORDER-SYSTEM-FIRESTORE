/** Firestore + Storage settings (no Google Sheet / Drive) */
window.APP_CONFIG = {
  /** Firestore collection for catalog items */
  PRODUCTS_COLLECTION: "products",
  /** Storage folder prefix for product images */
  STORAGE_PRODUCTS_PREFIX: "products"
};
