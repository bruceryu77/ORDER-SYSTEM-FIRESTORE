rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /products/{productId} {
      allow read: if request.auth != null;
      // 당분간 로그인 사용자 쓰기 허용 (나중에 admin만으로 좁히세요)
      allow write: if request.auth != null;
    }
  }
}
